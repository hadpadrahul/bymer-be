# Plan 05-03: Inquiries, media, home, email, audit, API reference

## Objective

Submissions inbox, media library, dashboard home warnings, email notifications, audit logging, API reference page, and polish.

## Tasks

### T1 — Submissions
- Contact + career list views (filters: status, search name/email)
- Detail view: status dropdown, internal notes, no delete
- CSV export view
- `/api/admin/` PATCH for status/notes (optional if form-only)

### T2 — Email notifications
- On public POST contact/career: email admins from `ADMIN_NOTIFICATION_EMAILS`
- Plain text summary with link to dashboard detail

### T3 — Media library
- Upload view → returns/stores file under `MEDIA_ROOT`, shows URL
- Grid of recent uploads (scan or track `MediaUpload` model if needed — prefer simple file log table only if required)
- Link from library to edit owning record when detectable

### T4 — Dashboard home
- `health_checks.py`: missing images, inactive flags, empty categories, etc.
- Recent submissions table
- Count cards

### T5 — API reference page
- `/dashboard/api-reference/` — table of public endpoints + link to `/api/docs/`

### T6 — Audit log
- Mixin on dashboard write views to create `AdminAuditEntry`
- Optional read-only list for superusers

### T7 — Preview links
- Use `PUBLIC_WEBSITE_BASE_URL` + page slug on page detail

### T8 — Tests + docs
- Export CSV test
- Email sent mock test
- Update `docs/DEVELOPMENT.md` with dashboard run instructions (no GSD mentions in user docs)

## Verification

- Home warnings render with seeded demo gaps
- CSV export downloads
- Email fires on test submission
- Audit entries created on edit
