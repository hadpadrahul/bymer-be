---
phase: 3
phase_name: Public API Contract
plan: 03-03
title: Catalog APIs and Page Composition
type: execute
wave: 3
depends_on: [03-01, 03-02]
autonomous: true
requirements: [PAGE-03, PAGE-04, CAT-04, CAT-05, CAT-06, API-02, API-06]
requirements_addressed: [PAGE-03, PAGE-04, CAT-04, CAT-05, CAT-06, API-02, API-06]
files_modified:
  - catalog/serializers.py
  - catalog/views.py
  - catalog/urls.py
  - catalog/filters.py
  - catalog/tests/test_api.py
  - pages/serializers.py
  - pages/views.py
  - pages/page_compose.py
  - pages/urls.py
  - pages/tests/test_api.py
  - config/urls.py
must_haves:
  truths:
    - "D-07: Page composition uses slug-keyed mapping, not a page builder."
    - "D-08: Products and machinery support category and plant query filters."
    - "PAGE-04: Optional page fields and empty sections are omitted safely."
  artifacts:
    - "catalog/serializers.py"
    - "catalog/views.py"
    - "catalog/filters.py"
    - "pages/page_compose.py"
    - "pages/views.py"
  key_links:
    - "PAGE-03 -> GET /api/pages/<slug>/ composed payload"
    - "CAT-04 -> products filtered by category slug"
    - "CAT-05 -> machinery filtered by plant"
    - "CAT-06 -> deterministic ordering on catalog lists"
---

<objective>
Implement catalog read APIs with category and plant filtering, plus the page composition endpoint that returns page metadata, banners, and embedded section data for frontend rendering.
</objective>

<context>
Read `.planning/phases/03-public-api-contract/03-CONTEXT.md`, `03-01-PLAN.md`, `03-02-PLAN.md`, `pages/models.py`, `catalog/models.py`, and `bymer_project_info.md` before editing.
</context>

<threat_model>

## Scope

Public catalog list APIs and page composition endpoint.

## STRIDE Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01 | Denial of Service | Heavy page composition | Low | Mitigate | Limit sections per slug; prefetch querysets; avoid unbounded joins. |

</threat_model>

<tasks>

<task id="T1" type="execute">
<title>Add catalog serializers, filters, and read APIs</title>
<read_first>
- `catalog/models.py`
- `content/views.py`
- `core/api/mixins.py`
</read_first>
<files>
- catalog/serializers.py
- catalog/filters.py
- catalog/views.py
- catalog/urls.py
- config/urls.py
</files>
<action>
Add read serializers for `ProductCategory`, `Product`, and `Machinery` with absolute image URLs. Add `ProductFilter` with `category` slug param on product list. Add `MachineryFilter` with `plant` param. Implement read-only viewsets for categories, products, and machinery with `ActiveQuerysetMixin`, pagination, and `select_related("category")` on products. Wire `catalog/urls.py` and mount at `api/catalog/` in `config/urls.py`. Add schema annotations.
</action>
<verify>
- `pytest catalog/tests/test_api.py` (create minimal tests in T2 if file not yet present — or split: write tests in T2)
- `python manage.py spectacular --file schema.yml --validate`
</verify>
<acceptance_criteria>
- `catalog/serializers.py` contains three catalog serializers.
- `GET /api/catalog/products/?category=<slug>` filters by category slug.
- `GET /api/catalog/machinery/?plant=plant_1` filters machinery.
- Inactive catalog rows are excluded from lists.
- Products list uses `select_related("category")` in queryset.
</acceptance_criteria>
</task>

<task id="T2" type="execute">
<title>Add catalog API tests</title>
<read_first>
- `catalog/views.py`
- `content/tests/test_api.py`
</read_first>
<files>
- catalog/tests/test_api.py
</files>
<action>
Add API tests for category/product/machinery lists, active-only behavior, category slug filter, plant filter, and stable ordering in response payloads.
</action>
<verify>
- `pytest catalog/tests/test_api.py`
</verify>
<acceptance_criteria>
- `catalog/tests/test_api.py` tests category and plant filters.
- `pytest catalog/tests/test_api.py` exits 0.
</acceptance_criteria>
</task>

<task id="T3" type="execute">
<title>Implement page composition view</title>
<read_first>
- `pages/models.py`
- `site_settings/models.py`
- `content/serializers.py`
- `catalog/serializers.py`
- `.planning/phases/03-public-api-contract/03-CONTEXT.md`
</read_first>
<files>
- pages/page_compose.py
- pages/serializers.py
- pages/views.py
- pages/urls.py
- config/urls.py
</files>
<action>
Create `pages/page_compose.py` with a slug-to-sections mapping for known site pages from `bymer_project_info.md`. Implement `PageDetailView` at `GET /api/pages/<slug>/` that loads active `WebsitePage`, attaches active ordered banners for the page (and optionally global banners with null page if useful), builds `sections` array with `{ "type": "<section_type>", "data": [...] }`, and omits empty optional fields. Return 404 for unknown/inactive slugs. Reuse content/catalog serializers for embedded `data`. Use prefetch to avoid N+1. Wire `pages/urls.py` at `api/pages/`.
</action>
<verify>
- `python manage.py check`
- `python manage.py spectacular --file schema.yml --validate`
</verify>
<acceptance_criteria>
- `pages/page_compose.py` exists with slug mapping for multiple known pages.
- `pages/views.py` exposes `PageDetailView` or equivalent.
- `config/urls.py` contains `path("api/pages/", include("pages.urls"))`.
- Inactive `WebsitePage` returns 404 on page endpoint.
- Empty optional metadata keys are omitted when blank.
</acceptance_criteria>
</task>

<task id="T4" type="execute">
<title>Add page API tests</title>
<read_first>
- `pages/views.py`
- `pages/page_compose.py`
</read_first>
<files>
- pages/tests/test_api.py
</files>
<action>
Add tests: active page returns 200 with `slug`, `title`, `sections`; unknown slug 404; inactive page 404; at least one mapped slug returns expected section `type`; empty optional fields not present when unset.
</action>
<verify>
- `pytest pages/tests/test_api.py`
</verify>
<acceptance_criteria>
- `pages/tests/test_api.py` covers success and 404 cases.
- Tests assert `sections` is a list with `type` keys for a configured slug.
- `pytest pages/tests/test_api.py` exits 0.
</acceptance_criteria>
</task>

</tasks>

<verification>

1. `python manage.py check`
2. `python manage.py spectacular --file schema.yml --validate`
3. `pytest catalog pages/tests/test_api.py`

</verification>

<success_criteria>

- Catalog endpoints support category and plant filtering with active-only ordered lists.
- Page endpoint returns composed frontend-ready payloads.
- Catalog and page API tests pass.

</success_criteria>
