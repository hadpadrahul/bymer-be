---
phase: 3
phase_name: Public API Contract
plan: 03-02
title: Content Collection Read APIs
type: execute
wave: 2
depends_on: [03-01]
autonomous: true
requirements: [CONT-08, API-01, API-02, API-03, API-04, API-05, API-06]
requirements_addressed: [CONT-08, API-01, API-02, API-03, API-04, API-05, API-06]
files_modified:
  - content/serializers.py
  - content/views.py
  - content/urls.py
  - content/filters.py
  - content/tests/test_api.py
  - config/urls.py
must_haves:
  truths:
    - "D-02: Content collections live under `/api/content/`."
    - "D-03: Content lists return active ordered records."
    - "D-08: Team and testimonial endpoints support required query filters."
    - "D-09: Growing collections use pagination."
  artifacts:
    - "content/serializers.py"
    - "content/views.py"
    - "content/filters.py"
    - "content/urls.py"
  key_links:
    - "CONT-08 -> read-only endpoints for all seven content models"
    - "API-02 -> TeamMemberFilter pillar, TestimonialDocumentFilter type"
    - "API-03 -> pagination on content list endpoints"
---

<objective>
Expose read-only REST collection endpoints for all repeatable content models with filtering, pagination, lean serializers, and API tests.
</objective>

<context>
Read `.planning/phases/03-public-api-contract/03-CONTEXT.md`, `.planning/phases/03-public-api-contract/03-RESEARCH.md`, `03-01-PLAN.md`, `content/models.py`, and `core/api/mixins.py` before editing. Reuse shared API helpers from plan 03-01.
</context>

<threat_model>

## Scope

Public read APIs for team, timeline, clients, testimonials, certifications, awards, and FAQs.

## STRIDE Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01 | Information Disclosure | Unpublished content | Low | Mitigate | Active-only queryset on all list endpoints. |

</threat_model>

<tasks>

<task id="T1" type="execute">
<title>Add content serializers and filters</title>
<read_first>
- `content/models.py`
- `core/api/mixins.py`
- `core/api/fields.py`
- `bymer_project_info.md`
</read_first>
<files>
- content/serializers.py
- content/filters.py
</files>
<action>
Create lean read serializers for `TeamMember`, `TimelineEvent`, `ClientPartner`, `TestimonialDocument`, `Certification`, `Award`, and `FAQ`. Use absolute URLs for image/file fields. Add `TeamMemberFilter` with boolean `pillar` query param mapping to `is_management_pillar`. Add `TestimonialDocumentFilter` with `type` choice filter. No admin-only fields in public serializers.
</action>
<verify>
- `python manage.py check`
</verify>
<acceptance_criteria>
- `content/serializers.py` contains seven public read serializers.
- `content/filters.py` contains filters for team pillar and testimonial type.
- Serializers expose `order` and stable field names for frontend arrays.
</acceptance_criteria>
</task>

<task id="T2" type="execute">
<title>Add read-only content viewsets</title>
<read_first>
- `content/serializers.py`
- `content/filters.py`
- `site_settings/views.py`
</read_first>
<files>
- content/views.py
</files>
<action>
Implement `ReadOnlyModelViewSet` (or equivalent) per content model with routes: `team`, `timelines`, `clients`, `testimonials`, `certifications`, `awards`, `faqs`. Apply `ActiveQuerysetMixin`, pagination, and filters where defined. Preserve model ordering. Add `@extend_schema` annotations.
</action>
<verify>
- `python manage.py check`
</verify>
<acceptance_criteria>
- `content/views.py` defines viewsets for all seven content models.
- Team viewset uses `TeamMemberFilter`.
- Testimonial viewset uses `TestimonialDocumentFilter`.
- Inactive records are excluded from list responses.
</acceptance_criteria>
</task>

<task id="T3" type="execute">
<title>Wire content URLs</title>
<read_first>
- `config/urls.py`
- `site_settings/urls.py`
</read_first>
<files>
- content/urls.py
- config/urls.py
</files>
<action>
Create `content/urls.py` with a `DefaultRouter` registering all content viewsets. Mount at `path("api/content/", include("content.urls"))` in `config/urls.py`.
</action>
<verify>
- `python manage.py spectacular --file schema.yml --validate`
</verify>
<acceptance_criteria>
- `config/urls.py` contains `path("api/content/", include("content.urls"))`.
- `GET /api/content/team/` is routable.
- `python manage.py spectacular --file schema.yml --validate` exits 0.
</acceptance_criteria>
</task>

<task id="T4" type="execute">
<title>Add content API tests</title>
<read_first>
- `content/views.py`
- `site_settings/tests/test_api.py`
</read_first>
<files>
- content/tests/test_api.py
</files>
<action>
Add tests: list endpoints return 200; inactive records excluded; `?pillar=true` filters team members; `?type=customer` filters testimonials; paginated list shape includes `results` key if using page pagination; response arrays preserve ordering; representative serializer keys present.
</action>
<verify>
- `pytest content/tests/test_api.py`
</verify>
<acceptance_criteria>
- `content/tests/test_api.py` covers active filtering and at least both required filters.
- `pytest content/tests/test_api.py` exits 0.
</acceptance_criteria>
</task>

</tasks>

<verification>

1. `python manage.py check`
2. `python manage.py spectacular --file schema.yml --validate`
3. `pytest site_settings content/tests/test_api.py`

</verification>

<success_criteria>

- All seven content collections are available under `/api/content/`.
- Filters work for team pillar and testimonial type.
- Lists are paginated, active-only, and ordered.
- Content API tests pass.

</success_criteria>
