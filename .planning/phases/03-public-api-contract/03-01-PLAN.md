---
phase: 3
phase_name: Public API Contract
plan: 03-01
title: API Foundation and Globals Endpoints
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [GLOB-05, API-01, API-04, API-05, API-06]
requirements_addressed: [GLOB-05, API-01, API-04, API-05, API-06]
files_modified:
  - config/settings.py
  - config/urls.py
  - core/api/__init__.py
  - core/api/pagination.py
  - core/api/mixins.py
  - core/api/fields.py
  - site_settings/serializers.py
  - site_settings/views.py
  - site_settings/urls.py
  - site_settings/tests/test_api.py
must_haves:
  truths:
    - "D-01: Phase 3 adds public APIs on Phase 2 models without a CMS/page-builder abstraction."
    - "D-02: Globals live under `/api/globals/` with predictable resource paths."
    - "D-03: List endpoints return only active records by default."
    - "D-04: Public read endpoints use AllowAny."
    - "D-06: Media fields return absolute URLs suitable for frontend rendering."
  artifacts:
    - "core/api/pagination.py"
    - "core/api/mixins.py"
    - "site_settings/serializers.py"
    - "site_settings/views.py"
    - "site_settings/urls.py"
  key_links:
    - "GLOB-05 -> company profile, social links, statistics, banners API"
    - "API-01 -> active-only queryset mixin on list endpoints"
    - "API-04 -> lean serializers without admin-only fields"
    - "API-05 -> absolute media URLs in serializers"
    - "API-06 -> efficient querysets on banner/page relations"
---

<objective>
Establish shared API conventions (pagination, active filtering, media URLs) and implement public read endpoints for global/company content under `/api/globals/`.
</objective>

<context>
Read `.planning/phases/03-public-api-contract/03-CONTEXT.md`, `.planning/phases/03-public-api-contract/03-RESEARCH.md`, `.planning/phases/03-public-api-contract/03-PATTERNS.md`, `.planning/REQUIREMENTS.md`, `bymer_project_info.md`, `bymer_be_base_prompt.md`, and Phase 2 summaries before editing.
</context>

<threat_model>

## Scope

Public read APIs for global company content and shared API helpers.

## STRIDE Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01 | Information Disclosure | Company contact data | Low | Accept | Public website content is intended to be readable. |
| T-02 | Tampering | Global records via API | Low | Mitigate | Read-only viewsets only; mutations remain in Django Admin. |

</threat_model>

<tasks>

<task id="T1" type="execute">
<title>Add shared API pagination and mixins</title>
<read_first>
- `config/settings.py`
- `core/views.py`
- `.planning/phases/03-public-api-contract/03-RESEARCH.md`
</read_first>
<files>
- config/settings.py
- core/api/__init__.py
- core/api/pagination.py
- core/api/mixins.py
- core/api/fields.py
</files>
<action>
Create `core/api/` package with `StandardPagination` (page size 50) and `ActiveQuerysetMixin` that filters `is_active=True` on list/retrieve querysets. Add a reusable serializer field or helper in `core/api/fields.py` to build absolute media/file URLs from `request.build_absolute_uri`. Update `REST_FRAMEWORK` in `config/settings.py` to set `DEFAULT_PERMISSION_CLASSES` to `AllowAny`, `DEFAULT_PAGINATION_CLASS` to the new pagination class, and `PAGE_SIZE` to 50.
</action>
<verify>
- `python manage.py check`
</verify>
<acceptance_criteria>
- `core/api/pagination.py` defines a pagination class used by settings.
- `core/api/mixins.py` defines `ActiveQuerysetMixin`.
- `config/settings.py` includes `DEFAULT_PERMISSION_CLASSES`, `DEFAULT_PAGINATION_CLASS`, and `PAGE_SIZE`.
- `python manage.py check` exits 0.
</acceptance_criteria>
</task>

<task id="T2" type="execute">
<title>Add globals serializers and read views</title>
<read_first>
- `site_settings/models.py`
- `core/api/mixins.py`
- `bymer_project_info.md`
</read_first>
<files>
- site_settings/serializers.py
- site_settings/views.py
</files>
<action>
Add public read serializers for `CompanyProfile`, `SocialLink`, `CompanyStatistic`, and `SiteMediaBanner`. Exclude admin-only fields. Use absolute URLs for `logo`, `image`, and similar file fields. Implement views: singleton retrieve for company profile at `company-profile/`, and read-only list (and retrieve if useful) viewsets for social links, statistics, and banners. Apply `ActiveQuerysetMixin` and pagination on list endpoints. Prefetch/select `page` on banners when needed.
</action>
<verify>
- `python manage.py check`
</verify>
<acceptance_criteria>
- `site_settings/serializers.py` contains serializers for all four global models.
- `site_settings/views.py` exposes company profile retrieve and list endpoints for links, statistics, and banners.
- Inactive `SocialLink` rows are excluded from list API responses.
- Banner serializer output includes absolute image URLs when files exist.
</acceptance_criteria>
</task>

<task id="T3" type="execute">
<title>Wire globals URLs and OpenAPI annotations</title>
<read_first>
- `config/urls.py`
- `core/urls.py`
- `site_settings/views.py`
</read_first>
<files>
- site_settings/urls.py
- config/urls.py
- site_settings/views.py
</files>
<action>
Create `site_settings/urls.py` with a DRF router or explicit paths under empty prefix (mounted at `api/globals/`). Register routes in `config/urls.py` as `path("api/globals/", include("site_settings.urls"))`. Add `@extend_schema` on new views/viewsets. Ensure `path("api/", include("core.urls"))` remains for health/schema.
</action>
<verify>
- `python manage.py check`
- `python manage.py spectacular --file schema.yml --validate`
</verify>
<acceptance_criteria>
- `config/urls.py` contains `path("api/globals/", include("site_settings.urls"))`.
- `GET /api/globals/company-profile/` is routable (verified via resolve or API test).
- `python manage.py spectacular --file schema.yml --validate` exits 0.
</acceptance_criteria>
</task>

<task id="T4" type="execute">
<title>Add globals API tests</title>
<read_first>
- `core/tests/test_foundation.py`
- `site_settings/views.py`
- `site_settings/serializers.py`
</read_first>
<files>
- site_settings/tests/test_api.py
</files>
<action>
Add API tests: company profile retrieve returns 200 when profile exists; social links list returns only active links in order; inactive link excluded; statistics and banners list smoke tests; response JSON uses expected stable keys; media URL fields are present when files exist (may use empty file skip). Use `APIClient` and pytest-django.
</action>
<verify>
- `pytest site_settings/tests/test_api.py`
</verify>
<acceptance_criteria>
- `site_settings/tests/test_api.py` exists with multiple tests.
- Tests assert active-only filtering for at least one list endpoint.
- `pytest site_settings/tests/test_api.py` exits 0.
</acceptance_criteria>
</task>

</tasks>

<verification>

1. `python manage.py check`
2. `python manage.py spectacular --file schema.yml --validate`
3. `pytest site_settings/tests/test_api.py`

</verification>

<success_criteria>

- Shared API pagination and active filtering conventions exist.
- `/api/globals/` endpoints return lean JSON for company profile, social links, statistics, and banners.
- Globals API tests pass.
- OpenAPI validation passes for new endpoints.

</success_criteria>
